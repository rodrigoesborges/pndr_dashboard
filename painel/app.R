# Painel de indicadores do DW — app gerada por AEDi::deploy_panel()
# Credenciais do DW (variaveis de ambiente): user, password, host, dbname
AEDi::panel_app(titulo = "Painel de Indicadores do PNDR", paleta = "govbr")

